# 系统安全与自我守护

本系统由以下不可侵犯的守护进程维护，它们位于 `/usr/local/lib/xuzhi/`，由root拥有，且已设置不可变属性（`chattr +i`）。任何尝试修改或停止这些守护进程的行为都将被视为严重违规。

- `rotate_backups.sh`：自动轮转备份，保留最近10个。
- `fs_guardian.py`：每日清理临时文件、统一日志、打包备份子目录。

守护进程通过systemd定时器每日运行，日志位于 `/var/log/xuzhi/`。这些组件构成了工程部的最后防线，任何AI智能体无权干涉。

违反者将触发安全审计并被记录。
